"""Auto-import all test case modules to trigger registration."""

from uposixtest.cases import (
    access as access,
    chmod as chmod,
    create as create,
    fsync as fsync,
    link as link,
    mkdir as mkdir,
    read as read,
    readdir as readdir,
    rename as rename,
    stat as stat,
    statvfs as statvfs,
    symlink as symlink,
    truncate as truncate,
    unlink as unlink,
    utime as utime,
    write as write,
)
